def weirdsum(x,y):
	sum=0
	for a in range(x,y+1):
		if a % 3==0:
			sum=sum+a
		if a % 5==0:
			sum=sum+a
		if a % 5==0 and a%3==0:
			sum=sum-a
	return sum
	


def strangesum(n):
	sum=0
	for x in range(0,n):
		if x%3==0 or x%5==0:
			sum=sum+x
		elif x%3==0 and x%5==0:
			sum=sum-x
	return sum